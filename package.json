{
  "name": "placeholder",
  "private": true,
  "description": "Scholarly Notes — run backend + frontend together",
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix frontend",
    "preview": "concurrently -n api,web -c blue,green \"npm run dev --prefix backend\" \"npm run preview --prefix frontend\"",
    "lint": "npm run lint --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "dependencies": {
    "@tiptap/extension-code-block-lowlight": "^3.20.4",
    "lowlight": "^3.3.0"
  }
}
